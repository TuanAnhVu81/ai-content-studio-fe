import { RoutePlaceholder } from "@/components/common/RoutePlaceholder";

export function CampaignListPage() {
  return (
    <RoutePlaceholder
      eyebrow="Campaigns"
      title="Campaign list route is scaffolded"
      description="This page now sits behind the protected layout and is ready for TanStack Query, filters, pagination and CRUD mutations in Phase 2."
    />
  );
}
