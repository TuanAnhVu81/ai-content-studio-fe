import { RoutePlaceholder } from "@/components/common/RoutePlaceholder";

export function AdminContentsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Admin Contents"
      title="Content moderation page scaffolded"
      description="Recent content review and hard-delete moderation will attach here in the admin phase."
    />
  );
}
