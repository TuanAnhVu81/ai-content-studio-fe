import { RoutePlaceholder } from "@/components/common/RoutePlaceholder";

export function AdminCampaignsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Admin Campaigns"
      title="Campaign monitor page scaffolded"
      description="Read-only monitoring for all campaigns can plug into this page with the existing admin guard and main layout."
    />
  );
}
