import { RoutePlaceholder } from "@/components/common/RoutePlaceholder";

export function AdminUsersPage() {
  return (
    <RoutePlaceholder
      eyebrow="Admin Users"
      title="User management page scaffolded"
      description="This page will host search, filters and status mutations once the admin feature phase starts."
    />
  );
}
